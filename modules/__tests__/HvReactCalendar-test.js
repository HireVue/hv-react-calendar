var assert          = require('assert');
//var React           = require('react');
//var HvReactCalendar = require('../index');

describe ('HvReactCalendar', function() {

  describe ('when the forward button is pressed', function() {
    it ('goes to the following month', function() {
      assert(false);
      done();
    });
  });

  describe ('when the back button is pressed', function() {
    it ('goes to the previous month', function() {
      assert(false);
      done();
    });
  });

});
